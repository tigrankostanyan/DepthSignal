// ==========================================
// SHARED TYPE BARREL (single source of truth)
// Re-exports the canonical domain types from the root shared types module.
// Import from '@/types' (or '@/types/index') as before — no call-site changes.
// ==========================================

export * from '../../../server/src/types/index.js';
